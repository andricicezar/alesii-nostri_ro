{% extends 'base.html' %}

{% block content %}


<div class="container py-5">
  <div class="row mb-3">
    <div class="col-12">
      <h1>{{names[0]}}</h1>
      <h2>Judetul {{ names[2] }}{% if names[3] %}, {{ names[3] }} {% endif %}</h2>
      <h2>Candidati la {{ names[1] }}</h2>
    </div>
  </div>
  <div class="row">
    <div class="col-12 col-md">
      <table id="dtBasicExample" class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
        <thead>
          <tr>
            <th class="th-sm">Nume complet
            </th>
            <th class="th-sm">Partid
            </th>
            <th class="th-sm">Pozitie pe lista
            </th>
            <!-- <th class="th-sm">Statut-->
            <!-- </th>-->
            <!-- <th class="th-sm">Linkuri-->
            <!-- </th>-->
          </tr>
        </thead>
        <tbody>
          {% for candidate in candidates %}
          <tr>
            <td>{{ candidate[0] }}</td>
            <td>{{ candidate[1] }}</td>
            <td>{{ candidate[2] }}</td>
            <!-- <td>{{ candidate[3] }}</td>-->
            <!-- <td></td>-->
          </tr>
          {% endfor %}
        </tbody>
      </table>


    </div>
  </div>
</div>

{% endblock %}
